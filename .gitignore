# Dependencies
node_modules/
.pnpm-store/

# Env & secrets (NEVER commit real secrets — only .env.example)
.env
.env.*
!.env.example

# Build outputs
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Test / coverage
coverage/
.nyc_output/

# Terraform / infra
infra/**/.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Prisma generated client
packages/db/generated/

# Local data volumes / media
/data/
recordings/
uploads/
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Build kit archive (kept locally, not in repo)
VocalIQ-Build-Kit.zip

# Word/Office lock files
~$*

# iCloud sync conflict copies (repo lives in an iCloud-synced folder)
* [0-9].*

# i18n translator hand-off — regenerable outputs (run scripts/i18n/export.mjs)
docs/i18n/out/
