/**
 * @vocaliq/db — Prisma schema, migrations, RLS policies, and generated client.
 * The authoritative multi-tenant schema lands on Day 4 (DATA-MODEL.md), along
 * with RLS policies in `rls/` and the cross-tenant isolation test (Day 5).
 * This Day-0 stub only marks the package boundary.
 */
export const DB_PACKAGE = '@vocaliq/db' as const;
